spring:
  application:
    name: automation-service
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
  datasource:
    url: jdbc:mysql://localhost:3306/automation_db
    username: root
    password: Ijse@1234
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka server location
    register-with-eureka: true
    fetch-registry: true